{% extends 'base.html.twig' %}

{% block body %}


<div class="container-fluid" style="position: relative; width:70%;" >
 <h1><b>Crear Persona </b></h1><br />
    {{ form_start(form) }}
        {{ form_widget(form) }}
     {{ form_errors(form) }}
        <input class="btn btn-outline-primary" type="submit" value="Crear" />
    {{ form_end(form) }}
  <br />
    <ul class="list-group">
        <li>
            <a href="{{ path('persona_index') }}">Volver a la lista</a>
        </li>
    </ul>
   
   </div> 
{% endblock %}
