{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editar Persona</h1></br>
<div class="container-fluid" style="position: relative; width:70%;" >
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}
 



    <ul>
        <li style="list-style:none;"><br />
            <a href="{{ path('persona_index') }}">Volver a la lista</a>
        </li><br />
         <li style="list-style:none;">
     <form method="get" action="{{ path('vacaciones_new', { 'id' : persona.id }) }}">
     <input type="submit" value="Agregar periodo vacaciones" />
     </form>
     </br>
    </li>
        <li style="list-style:none;">
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete"/>
            {{ form_end(delete_form) }}
        </li>

    </ul>
    </div>
{% endblock %}
